//create string with message
let string1= "You have received this message because you have been chosen to open an important vault. Here is the secret combination:";
//assign variables corresponding to each of the three codes in the combination
const one= 5+5;
const two= 50-10;
const three= 3*13;
//create a dialog box displaying the vault codes
let code= prompt(string1+one+"-"+two+"-"+three);